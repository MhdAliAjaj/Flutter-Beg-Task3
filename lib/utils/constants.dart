import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2979FF); // أزرق للـ Login button
  static const textGrey = Colors.grey;
  static const dark = Color(0xFF1E1E1E);
}
